1) JSX is not HTML inside JS, JSX is a HTML like syntax.
2) JS engine understands ECMAScript, does not understand JSX
3) Parcel transpiles (converted) JSX to browser understanding JS code
4) Parcel gives responsibiltied of transpiling to Babel
5) JSX => React.createElement => React Element (JS Object) => HTML element (render)
6) There are 2 types of components in React
    - Class based component
    - Functional component
7) React functionl component is just normal JS function which returns JSX.
8) Component Composition: Use of a component inside another
9) JSX prevents cross site scripting attack
